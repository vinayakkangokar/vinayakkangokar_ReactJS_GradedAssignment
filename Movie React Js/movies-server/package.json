{
  "name": "movie-server",
  "version": "1.0.0",
  "description": "Movie Application Server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch db.json --port 3001",
    "start:delay": "npm start -- --delay 2000"
  },
  "author": "Vinayak Kangokar",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
